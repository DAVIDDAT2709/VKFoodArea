@model VKFoodArea.Web.ViewModels.QrCodeItemIndexViewModel
@{
    ViewData["Title"] = "Mã QR";
    var items = Model.Items.Items;
}

<section class="page-header page-section">
    <div>
        <h2>Mã QR</h2>
    </div>
    <div class="page-actions">
        <a href="/download-app" target="_blank" rel="noopener" class="btn btn-outline-secondary">QR tải App</a>
        <a asp-action="Create" class="btn btn-primary">Tạo mã QR</a>
    </div>
</section>

<section class="metrics-grid page-section">
    <article class="metric-card metric-card--blue">
        <span class="metric-card__label">Tổng mã</span>
        <strong class="metric-card__value">@Model.TotalCount</strong>
    </article>
    <article class="metric-card metric-card--green">
        <span class="metric-card__label">Đang hoạt động</span>
        <strong class="metric-card__value">@Model.ActiveCount</strong>
    </article>
    <article class="metric-card metric-card--orange">
        <span class="metric-card__label">Liên kết POI</span>
        <strong class="metric-card__value">@Model.CoveredPoiCount</strong>
    </article>
    <article class="metric-card metric-card--red">
        <span class="metric-card__label">Liên kết Tour</span>
        <strong class="metric-card__value">@Model.CoveredTourCount</strong>
    </article>
</section>

<section class="card-surface page-section">
    <div class="section-heading">
        <h2>Kho mã QR</h2>
    </div>

    @if (!items.Any())
    {
        <div class="empty-state">
            <p>Chưa có mã QR.</p>
        </div>
    }
    else
    {
        <div class="inventory-grid">
            @foreach (var item in items)
            {
                var encodedCode = Uri.EscapeDataString(item.Code);
                <article class="inventory-card qr-card">
                    <div class="inventory-card__media">
                        <a class="qr-card__preview-link" href="@($"/qr/{encodedCode}")" target="_blank" rel="noopener"
                           aria-label="Mở link QR @item.Title">
                            <img src="@($"/qr-image/{encodedCode}")" alt="Mã QR @item.Title" loading="lazy" />
                        </a>
                    </div>

                    <div class="inventory-card__body">
                        <div class="inventory-card__header">
                            <div>
                                <div class="inventory-card__title">@item.Title</div>
                                <div class="inventory-card__meta">@item.TargetName</div>
                            </div>
                            @if (item.IsActive)
                            {
                                <span class="status-pill status-pill--success">Đang hoạt động</span>
                            }
                            else
                            {
                                <span class="status-pill status-pill--muted">Đang ẩn</span>
                            }
                        </div>

                        <div class="inventory-card__stats">
                            <div class="inventory-stat">
                                <strong>Mã</strong>
                                <span class="mono">@item.Code</span>
                            </div>
                            <div class="inventory-stat">
                                <strong>Đích đến</strong>
                                <span>@item.TargetType.ToUpperInvariant() #@item.TargetId</span>
                            </div>
                            <div class="inventory-stat">
                                <strong>Ngày tạo</strong>
                                <span>@WebDisplayTime.Format(item.CreatedAt, "dd/MM/yyyy HH:mm")</span>
                            </div>
                        </div>

                        <div class="inventory-card__actions">
                            <a href="@($"/qr/{encodedCode}")" target="_blank" rel="noopener" class="btn btn-outline-secondary btn-sm">Mở thử</a>
                            <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-outline-secondary btn-sm">Sửa</a>
                            <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-outline-danger btn-sm">Xóa</a>
                        </div>
                    </div>
                </article>
            }
        </div>
        <partial name="_Pagination"
                 model="@(VKFoodArea.Web.ViewModels.PaginationViewModel.From(Model.Items))" />
    }
</section>
