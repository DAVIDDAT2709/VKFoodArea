@model VKFoodArea.Web.ViewModels.QrCodeItemFormViewModel
@{
    ViewData["Title"] = "Cập nhật mã QR";
}

<section class="page-header page-section">
    <div>
        <h2>Cập nhật mã QR</h2>
    </div>
</section>

<form asp-action="Edit" method="post" enctype="multipart/form-data" class="stack-gap page-section">
    <input type="hidden" asp-for="Id" />
    <input type="hidden" asp-for="CurrentImageUrl" />
    <partial name="_Form" model="Model" />

    <div class="page-actions">
        <button type="submit" class="btn btn-primary">Lưu thay đổi</button>
        <a asp-action="Index" class="btn btn-outline-secondary">Quay lại</a>
    </div>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
