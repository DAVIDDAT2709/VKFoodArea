@model VKFoodArea.Web.ViewModels.QrCodeItemFormViewModel
@{
    ViewData["Title"] = "Cập nhật QR code";
}

<section class="page-intro page-section">
    <div>
        <span class="eyebrow">Chỉnh sửa mã</span>
        <h2>Cập nhật QR đang dùng cho luồng quét mở nội dung</h2>
        <p>Thay đổi mã hoặc POI liên kết sẽ ảnh hưởng trực tiếp đến trải nghiệm quét mã trong ứng dụng.</p>
    </div>
</section>

<form asp-action="Edit" method="post" class="stack-gap page-section">
    <input type="hidden" asp-for="Id" />
    <partial name="_Form" model="Model" />

    <div class="page-actions">
        <button type="submit" class="btn btn-primary">Lưu thay đổi</button>
        <a asp-action="Index" class="btn btn-outline-secondary">Quay lại danh sách</a>
    </div>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
